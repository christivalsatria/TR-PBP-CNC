info:
  name: Logout Kasir
  type: http
  seq: 7

http:
  method: POST
  url: http://localhost:5000/api/auth/logout
  body:
    type: json
    data: |-
      {
        "username": "AdminCNC",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​